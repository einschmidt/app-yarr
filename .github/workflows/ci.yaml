---
name: CI

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  packages: read
  security-events: write

jobs:
  workflows:
    uses: einschmidt/workflows/.github/workflows/app-ci.yaml@6a57fef88f7f2a778a99121a5f66a2c840c0d47f # v0.1.2
